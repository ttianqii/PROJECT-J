# PROJECT-J 技術ドキュメント

> **タイ人と日本人のためのAI発音学習プラットフォーム**

---

## 📋 目次

1. [プロジェクト概要](#-プロジェクト概要)
2. [システムアーキテクチャ](#-システムアーキテクチャ)
3. [必要条件](#-必要条件)
4. [インストールとセットアップ](#-インストールとセットアップ)
5. [開発ワークフロー](#-開発ワークフロー)
6. [バックエンドドキュメント](#-バックエンドドキュメント)
7. [フロントエンドドキュメント](#-フロントエンドドキュメント)
8. [APIリファレンス](#-apiリファレンス)
9. [コンポーネントライブラリ](#-コンポーネントライブラリ)
10. [データ構造](#-データ構造)
11. [デプロイメント](#-デプロイメント)
12. [トラブルシューティング](#-トラブルシューティング)

---

## 🌟 プロジェクト概要

PROJECT-Jは、タイ人と日本人がお互いの言語の発音を学べるフルスタックWebアプリケーションです。AI音声認識とリアルタイムフィードバックを活用しています。

### 主な機能

| 機能 | 説明 |
|------|------|
| **発音評価** | OpenAI Whisper + GPT-4oを使用した録音と精度スコアリング |
| **単語検索** | GPT-4o経由で任意の単語を検索し、音韻分解を取得 |
| **文のトークン化** | アクセント/トーンデータ付きで文を分解 |
| **フリースピークモード** | 制約なしで話す練習が可能 |
| **テキスト読み上げ** | ブラウザTTSを使用したネイティブ発音例 |
| **バイリンガルUI** | タイ語、日本語、英語に対応したインターフェース |

### 技術スタック

```
┌─────────────────────────────────────────────────────────────┐
│                        フロントエンド                        │
├─────────────────────────────────────────────────────────────┤
│  React 19 + TypeScript 5.9                                  │
│  Vite 7（ビルドツール）                                      │
│  TailwindCSS 4（スタイリング）                               │
│  Radix UI + Lucide React（コンポーネント＆アイコン）          │
│  Motion（アニメーション）                                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP / REST
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        バックエンド                          │
├─────────────────────────────────────────────────────────────┤
│  Elysia 1.4（BunベースのWebフレームワーク）                   │
│  OpenAI API（Whisper + GPT-4o）                             │
│  TypeScript 5                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏗️ システムアーキテクチャ

### ディレクトリ構造

```
PROJECT-J/
├── 📁 frontend/                    # Reactフロントエンドアプリケーション
│   ├── src/
│   │   ├── components/            # Reactコンポーネント
│   │   │   ├── smoothui/         # カスタムUIコンポーネント
│   │   │   ├── LanguageSelectScreen.tsx   # 初期言語選択
│   │   │   ├── PresetScreen.tsx           # 語彙プリセット選択
│   │   │   ├── WordCard.tsx               # 単語詳細表示
│   │   │   ├── PronunciationRecorder.tsx  # 録音インターフェース
│   │   │   ├── AccuracyFeedback.tsx       # 結果表示
│   │   │   ├── FreeSpeak.tsx              # フリー練習モード
│   │   │   ├── SentenceBreakdown.tsx      # 文分析
│   │   │   └── ...
│   │   ├── hooks/                # カスタムReactフック
│   │   │   ├── useAudioRecorder.ts
│   │   │   └── useTTS.ts
│   │   ├── services/             # API連携
│   │   │   └── api.ts
│   │   ├── utils/                # ユーティリティ関数
│   │   │   ├── i18n.ts          # 国際化
│   │   │   └── phonetics.ts     # 音響ヘルパー
│   │   ├── data/                 # 静的データ
│   │   │   ├── ja-th.json       # 日本語→タイ語語彙
│   │   │   └── th-ja.json       # タイ語→日本語語彙
│   │   ├── types/                # TypeScript型定義
│   │   │   └── index.ts
│   │   ├── App.tsx              # メインアプリケーションコンポーネント
│   │   └── main.tsx             # エントリーポイント
│   ├── index.html
│   ├── vite.config.ts
│   └── package.json
│
├── 📁 backend/                     # ElysiaバックエンドAPI
│   ├── src/
│   │   ├── routes/
│   │   │   └── pronunciation.ts  # APIルート
│   │   └── index.ts              # サーバーエントリーポイント
│   └── package.json
│
├── 📄 package.json                 # ルートワークスペース設定
├── 📄 .env                         # 環境変数
├── 📄 .env.example                 # 環境テンプレート
└── 📄 DOCUMENTATION.md             # 英語ドキュメント
```

---

## 📦 必要条件

開始する前に、以下がインストールされていることを確認してください：

| 要件 | バージョン | 目的 |
|------|-----------|------|
| **Bun** | 1.0+ | JavaScriptランタイム＆パッケージマネージャー |
| **Node.js** | 18+ | 互換性（Bunフォールバック） |
| **OpenAI API Key** | - | WhisperとGPT-4oに必要 |
| **モダンブラウザ** | Chrome/Firefox/Safari | MediaRecorder APIサポート |

### Bunのインストール

```bash
# macOS/Linux
curl -fsSL https://bun.sh/install | bash

# Windows（WSL経由が推奨）
curl -fsSL https://bun.sh/install | bash
```

### インストールの確認

```bash
bun --version    # 1.0以上が出力されるはず
```

---

## 🚀 インストールとセットアップ

### 1. クローンと移動

```bash
cd PROJECT-J
```

### 2. 依存関係のインストール

```bash
# すべての依存関係をインストール（フロントエンド＋バックエンド）
bun run install:all
```

これにより、Bunワークスペースを使用して両方のワークスペースの依存関係がインストールされます。

### 3. 環境変数の設定

```bash
# 例ファイルをコピー
cp .env.example .env

# OpenAI APIキーで.envを編集
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PORT=3001
```

> ⚠️ **重要**: `.env`をバージョン管理にコミットしないでください。すでに`.gitignore`に含まれています。

### 4. セットアップの確認

```bash
# バックエンドが起動できるか確認
bun run dev:backend

# 別のターミナルでフロントエンドを確認
bun run dev:frontend
```

---

## 💻 開発ワークフロー

### 両方のサービスを実行

```bash
# フロントエンド＋バックエンドを同時に起動
bun run dev
```

このコマンドは以下を実行します：
- フロントエンド：`http://localhost:5173`
- バックエンド：`http://localhost:3001`

### 個別にサービスを実行

```bash
# フロントエンドのみ
cd frontend && bun run dev

# バックエンドのみ
cd backend && bun run dev
```

### 本番用ビルド

```bash
# フロントエンドをビルド（frontend/distに出力）
bun run build
```

### 開発用URL

| サービス | URL | 説明 |
|---------|-----|------|
| フロントエンド | `http://localhost:5173` | React開発サーバー |
| バックエンドAPI | `http://localhost:3001/api` | REST APIエンドポイント |
| APIドキュメント | `http://localhost:3001/docs` | Swagger UIドキュメント |

---

## 🔧 バックエンドドキュメント

### アーキテクチャ

バックエンドは、パフォーマンスと型安全性に最適化されたBunネイティブWebフレームワーク**Elysia**で構築されています。

### エントリーポイント（`backend/src/index.ts`）

```typescript
import { Elysia } from 'elysia'
import { cors } from '@elysiajs/cors'
import { swagger } from '@elysiajs/swagger'

const app = new Elysia()
  .use(cors({...}))           // クロスオリジンリクエスト
  .use(swagger({...}))        // 自動生成APIドキュメント
  .get('/api/health', ...)    // ヘルスチェック
  .use(pronunciationRoutes)   // APIルート
  .listen(PORT)
```

### コアモジュール

#### 1. レーベンシュタイン距離アルゴリズム

期待されるテキストと音声認識結果の間の精度を計算するために使用：

```typescript
function levenshtein(a: string, b: string): number
// 戻り値: 2つの文字列間の編集距離
// 使用場所: calcAccuracy()
```

#### 2. 文字レベルの差分

文字レベルで発音エラーを視覚化：

```typescript
interface CharDiff {
  char: string
  status: 'correct' | 'wrong' | 'missing' | 'extra'
}
```

#### 3. GPT-4o分析

インテリジェントな発音指導を提供：

```typescript
interface AIAnalysis {
  score: number        // 0-100の音声スコア
  feedback: string     // 学習者の言語での指導ヒント
  mispronounced: string[]  // 問題のある音節
}
```

### 環境変数

| 変数 | 必須 | デフォルト | 説明 |
|------|------|-----------|------|
| `OPENAI_API_KEY` | ✅ | - | OpenAI APIキー |
| `PORT` | ❌ | 3001 | サーバーポート |

---

## 🎨 フロントエンドドキュメント

### プロジェクト構造

```
src/
├── components/          # UIコンポーネント
│   ├── smoothui/       # 再利用可能なカスタムUI
│   ├── LanguageSelectScreen.tsx   # 初期言語選択
│   ├── PresetScreen.tsx           # 語彙プリセット選択
│   ├── WordCard.tsx               # 単語詳細表示
│   ├── PronunciationRecorder.tsx  # 録音インターフェース
│   ├── AccuracyFeedback.tsx       # 結果表示
│   ├── FreeSpeak.tsx              # フリー練習モード
│   ├── SentenceBreakdown.tsx      # 文分析
│   └── BottomNav.tsx              # ナビゲーション
│
├── hooks/              # カスタムReactフック
│   ├── useAudioRecorder.ts   # MediaRecorderラッパー
│   └── useTTS.ts             # テキスト読み上げ
│
├── services/           # API統合
│   └── api.ts
│
├── utils/              # ヘルパー関数
│   ├── i18n.ts        # 翻訳
│   └── phonetics.ts   # 音響ユーティリティ
│
├── data/               # 静的JSONデータ
│   ├── ja-th.json     # 日本語語彙
│   └── th-ja.json     # タイ語語彙
│
└── types/              # TypeScript定義
    └── index.ts
```

### 主要コンポーネント

#### LanguageSelectScreen

初期言語選択インターフェースをレンダリングします。

**Props:**
```typescript
interface Props {
  mode: LearnerMode           // 'th-ja' | 'ja-th'
  appLang: AppLang            // 'th' | 'ja' | 'en'
  onSelect: (mode: LearnerMode) => void
  onContinue: () => void
  onAppLangChange: (lang: AppLang) => void
}
```

#### PronunciationRecorder

発音練習用のメイン録音インターフェース。

**使用方法:**
```tsx
<PronunciationRecorder
  entry={vocabEntry}
  mode="th-ja"
  onResult={(result) => console.log(result.aiScore)}
  onError={(msg) => console.error(msg)}
/>
```

#### WordCard

音響分解付きの語彙を表示します。

**機能:**
- ネイティブスクリプト表示
- ローマ字表記
- アクセント可視化（日本語）
- トーンインジケーター（タイ語）
- TTSによる音声再生

### カスタムフック

#### useAudioRecorder

オーディオキャプチャのMediaRecorderライフサイクルを管理します。

```typescript
const { state, start, stop, error, durationMs } = useAudioRecorder()

// State: 'idle' | 'recording' | 'processing'
// start(): Promise<void> - 録音開始
// stop(): Promise<Blob | null> - 録音終了、オーディオブロブを取得
```

#### useTTS

ブラウザテキスト読み上げラッパー。

```typescript
const { speak, speaking, supported } = useTTS()

speak('こんにちは', 'ja-JP')  // 日本語で話す
speak('สวัสดี', 'th-TH')      // タイ語で話す
```

---

## 📡 APIリファレンス

### ベースURL

```
開発環境: http://localhost:3001/api
本番環境:  https://your-domain.com/api
```

### エンドポイント

#### 1. ヘルスチェック

```http
GET /api/health
```

**レスポンス:**
```json
{
  "ok": true,
  "timestamp": "2026-02-25T07:51:06.459Z"
}
```

#### 2. 発音評価

```http
POST /api/assess
Content-Type: multipart/form-data
```

**パラメータ:**

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `audio` | File | オーディオブロブ（webm、ogg、mp4、wav） |
| `expectedWord` | string | ネイティブスクリプトのターゲット単語 |
| `expectedRoman` | string | ローマ字ガイド |
| `lang` | 'ja' \| 'th' | ターゲット言語 |

**リクエスト例:**
```javascript
const form = new FormData()
form.append('audio', audioBlob, 'recording.webm')
form.append('expectedWord', 'こんにちは')
form.append('expectedRoman', 'konnichiwa')
form.append('lang', 'ja')

fetch('/api/assess', {
  method: 'POST',
  body: form
})
```

**レスポンス:**
```json
{
  "ok": true,
  "transcribed": "こんにちは",
  "wordTimings": [
    { "word": "こんにちは", "start": 0.0, "end": 1.2 }
  ],
  "accuracy": 95,
  "charDiff": [
    { "char": "こ", "status": "correct" },
    { "char": "ん", "status": "correct" }
  ],
  "feedback": {
    "th": "🎉 ยอดเยี่ยมมาก!",
    "ja": "🎉 素晴らしい！"
  },
  "aiScore": 92,
  "aiFeedback": "การออกเสียงใกล้เคียงสำเนียงเจ้าของภาษา",
  "mispronounced": []
}
```

#### 3. 文字起こし（フリースピークモード）

```http
POST /api/transcribe
Content-Type: multipart/form-data
```

**パラメータ:**

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `audio` | File | オーディオブロブ |
| `lang` | 'ja' \| 'th' | ターゲット言語 |

**レスポンス:**
```json
{
  "ok": true,
  "transcribed": "おはようございます",
  "wordTimings": [...]
}
```

#### 4. 単語検索

```http
POST /api/lookup
Content-Type: application/json
```

**パラメータ:**

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `word` | string | 検索する単語 |
| `lang` | 'ja' \| 'th' | ソース言語 |

**リクエスト:**
```json
{
  "word": "ありがとう",
  "lang": "ja"
}
```

**レスポンス:**
```json
{
  "ok": true,
  "entry": {
    "id": "ai-1234567890",
    "category": "AI Lookup",
    "word": "ありがとう",
    "reading": "ありがとう",
    "romanization": "arigatou",
    "syllables": [...],
    "meaningTh": "ขอบคุณ",
    "meaningJa": "感謝している",
    "exampleSentence": "ありがとうございます",
    "exampleTranslation": "ขอบคุณค่ะ/ครับ",
    "ttsLang": "ja-JP"
  }
}
```

#### 5. 文のトークン化

```http
POST /api/tokenize
Content-Type: application/json
```

**パラメータ:**

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `sentence` | string | トークン化する文 |
| `lang` | 'ja' \| 'th' | ソース言語 |

**リクエスト:**
```json
{
  "sentence": "私は学生です",
  "lang": "ja"
}
```

**レスポンス:**
```json
{
  "ok": true,
  "tokens": [
    {
      "word": "私",
      "reading": "わたし",
      "romanization": "watashi",
      "isParticle": false,
      "meaningTh": "ฉัน/ผม",
      "meaningJa": "自分自身",
      "syllables": [...]
    }
  ],
  "translationTh": "ฉันเป็นนักเรียน",
  "translationJa": "私は学生です"
}
```

---

## 🧩 コンポーネントライブラリ

### SmoothUIコンポーネント

`frontend/src/components/smoothui/`に配置

#### SiriOrb

アニメーション付き音声可視化コンポーネント。

```tsx
import { SiriOrb } from './components/smoothui/siri-orb'

<SiriOrb 
  state="listening"  // 'idle' | 'listening' | 'speaking'
  size={120}
/>
```

#### AIInput

展開可能なAI入力インターフェース。

```tsx
import { AIInput } from './components/smoothui/ai-input'

<AIInput
  placeholder="単語を入力..."
  onSubmit={(value) => console.log(value)}
/>
```

### カスタムアイコン

プロジェクトでは**Lucide React**をアイコンとして使用：

```tsx
import { Mic, Volume2, Loader2, XCircle } from 'lucide-react'

<Mic size={24} />
<Volume2 className="text-red-400" />
```

---

## 📊 データ構造

### コア型

```typescript
// 言語方向
type LearnerMode = 'th-ja' | 'ja-th'

// UI言語
type AppLang = 'th' | 'ja' | 'en'

// 日本語アクセント
type PitchType = 'flat' | 'atamadaka' | 'nakadaka' | 'odaka' | 'heiban'

// タイ語トーンクラス
type ToneName = 'mid' | 'low' | 'falling' | 'high' | 'rising'
```

### 日本語音節構造

```typescript
interface PitchSyllable {
  kana: string        // 「た」「べ」「も」「の」
  roman: string       // "ta", "be", "mo", "no"
  isHigh: boolean     // true = 高音
  isAccentDrop: boolean  // true = このモーラの後にピッチが下がる
  thai?: string       // タイ語音声ヒント: "ทา", "เบ"
}
```

### タイ語音節構造

```typescript
interface ThaiSyllable {
  thai: string        // "สวัส"
  roman: string       // "sawat"
  tone: ToneName      // "falling"
  katakana?: string   // 日本語ヒント: "サワット"
}
```

### 語彙エントリ

```typescript
interface VocabEntry {
  id: string
  category: string
  word: string           // ネイティブスクリプト
  reading: string        // ひらがな/カタカナまたはタイ語
  romanization: string   // ローマ字またはRTGS
  ipa?: string          // オプションのIPA
  syllables: PitchSyllable[] | ThaiSyllable[]
  meaningTh: string     // タイ語訳
  meaningJa: string     // 日本語定義
  exampleSentence: string
  exampleTranslation: string
  ttsLang: 'ja-JP' | 'th-TH'
  notes?: string
}
```

### 評価レスポンス

```typescript
interface AssessResponse {
  ok: boolean
  transcribed: string
  wordTimings: WordTiming[]
  accuracy: number           // 0-100（レーベンシュタインベース）
  charDiff: CharDiffToken[]
  feedback: { th: string; ja: string }
  aiScore: number           // 0-100（GPT-4oスコア）
  aiFeedback: string        // 母語での指導
  mispronounced: string[]   // 問題のある音
  error?: string
}
```

---

## 🚢 デプロイメント

### 本番ビルド

```bash
# 1. フロントエンドをビルド
bun run build

# 2. 本番環境変数を設定
export OPENAI_API_KEY=sk-...
export PORT=3001

# 3. バックエンドを起動（API + 静的ファイルを提供）
cd backend && bun run start
```

### Dockerデプロイメント（オプション）

```dockerfile
# Dockerfile
FROM oven/bun:1

WORKDIR /app

# パッケージファイルをコピー
COPY package.json bun.lock ./
COPY frontend/package.json ./frontend/
COPY backend/package.json ./backend/

# 依存関係をインストール
RUN bun install

# ソースをコピー
COPY . .

# フロントエンドをビルド
RUN bun run build

# ポートを公開
EXPOSE 3001

# 起動
CMD ["bun", "run", "--env-file", ".env", "backend/src/index.ts"]
```

### 環境セットアップ

```bash
# イメージをビルド
docker build -t project-j .

# コンテナを実行
docker run -p 3001:3001 \
  -e OPENAI_API_KEY=sk-... \
  -e PORT=3001 \
  project-j
```

---

## 🔍 トラブルシューティング

### 一般的な問題

#### 1. "OPENAI_API_KEY is not set"

**解決策:**
```bash
# .envファイルを作成
cp .env.example .env

# キーを追加
echo "OPENAI_API_KEY=sk-..." > .env
```

#### 2. CORSエラー

**症状:** ブラウザで`Access-Control-Allow-Origin`エラー

**解決策:** バックエンドCORSがフロントエンドURL用に設定されていることを確認：

```typescript
// backend/src/index.ts
.use(cors({
  origin: ['http://localhost:5173', 'https://yourdomain.com'],
  methods: ['GET', 'POST', 'OPTIONS'],
}))
```

#### 3. マイクが動作しない

**チェックリスト:**
- [ ] ブラウザがMediaRecorder APIをサポートしている
- [ ] HTTPSまたはlocalhost（マイクアクセスに必要）
- [ ] ブラウザでマイク許可が付与されている
- [ ] 他のアプリがマイクを使用していない

#### 4. Whisper APIエラー

**症状:** 「無効なファイル形式」または文字起こしに失敗

**解決策:** 
- オーディオブロブに適切な拡張子（`.webm`、`.ogg`、`.mp4`、`.wav`）があることを確認
- MIMEタイプがサポートされているか確認

#### 5. ビルド失敗

```bash
# キャッシュをクリアして再インストール
rm -rf node_modules bun.lock
bun install
```

### デバッグモード

詳細ログを有効化：

```bash
# バックエンド
debug=true bun run dev:backend

# フロントエンド（ブラウザDevTools）
localStorage.setItem('debug', 'true')
```

---

## 📝 開発ガイドライン

### コードスタイル

- **TypeScript**: 厳格モード有効
- **Linting**: TypeScriptルール付きESLint
- **フォーマット**: 一貫した2スペースインデント

### 新しい語彙の追加

`frontend/src/data/`のJSONファイルを編集：

```json
// ja-th.json または th-ja.json
{
  "id": "greetings-hello",
  "category": "Greetings",
  "word": "こんにちは",
  "reading": "こんにちは",
  "romanization": "konnichiwa",
  "syllables": [
    { "kana": "こん", "roman": "kon", "isHigh": false, "isAccentDrop": false, "thai": "คอน" },
    { "kana": "に", "roman": "ni", "isHigh": true, "isAccentDrop": true, "thai": "นิ" },
    { "kana": "ち", "roman": "chi", "isHigh": false, "isAccentDrop": false, "thai": "ชิ" },
    { "kana": "は", "roman": "wa", "isHigh": false, "isAccentDrop": false, "thai": "วะ" }
  ],
  "meaningTh": "สวัสดี",
  "meaningJa": "昼の挨拶",
  "exampleSentence": "こんにちは、元気ですか？",
  "exampleTranslation": "สวัสดี สบายดีไหมครับ/คะ？",
  "ttsLang": "ja-JP"
}
```

### APIエンドポイントの追加

```typescript
// backend/src/routes/pronunciation.ts
export const pronunciationRoutes = new Elysia({ prefix: '/api' })
  .post('/new-endpoint', 
    async ({ body }) => {
      // 実装
      return { ok: true, data: ... }
    },
    {
      body: t.Object({
        field: t.String()
      })
    }
  )
```

---

## 📚 参考資料

### 学術的基盤

このプロジェクトは、Computer-Assisted Pronunciation Training（CAPT）の研究に基づいています：

1. **Golonka et al. (2014)** - 外国語学習技術のレビュー
2. **Neri et al. (2002)** - CAPTにおける教育法と技術のインターフェース

完全な引用については`ACADEMIC.md`を参照してください。

### 外部API

- [OpenAI Whisper](https://platform.openai.com/docs/guides/speech-recognition)
- [OpenAI GPT-4o](https://platform.openai.com/docs/models/gpt-4o)
- [Elysiaドキュメント](https://elysiajs.com/)

---

## 📄 ライセンス

このプロジェクトは教育目的です。

---

## 🤝 貢献

1. リポジトリをフォーク
2. 機能ブランチを作成
3. 変更を加える
4. プルリクエストを送信

---

## 💬 サポート

質問や問題がある場合：
1. トラブルシューティングセクションを確認
2. 既存のIssueを確認
3. 詳細な情報を含む新しいIssueを作成

---

*最終更新: 2026-02-25*
